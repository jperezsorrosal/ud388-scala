h2testdb = {
  url = "jdbc:h2:mem:testdb"
  driver = org.h2.Driver
  connectionPool = disabled
  keepAliveConnection = true
}

h2testdbUsers = {
  url = "jdbc:h2:mem:testdbusers"
  driver = org.h2.Driver
  connectionPool = disabled
  keepAliveConnection = true
}